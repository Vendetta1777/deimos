# Python virtual environment (large, machine-specific)
.venv/
venv/

# Bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Downloaded neural voice models (large binaries; see README to fetch)
voices/
*.onnx
*.onnx.json

# OS / editor cruft
.DS_Store
.vscode/
*.log

# Secrets / credentials — never commit these
.env
.spotify.json
token.json
client_secret*.json
credentials.json
*.db

# Deimos runtime data lives in ~/.deimos (outside the repo) — never commit it.
